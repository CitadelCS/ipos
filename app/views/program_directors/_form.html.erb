<%= form_with(model: program_director) do |form| %>
  <% if program_director.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(program_director.errors.count, "error") %> prohibited this program_director from being saved:</h2>

      <ul>
        <% program_director.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Name %>
    <%= form.text_field :Name %>
  </div>

  <div class="field">
    <%= form.label :Department %>
    <%= form.text_field :Department %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
