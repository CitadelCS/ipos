<p id="notice"><%= notice %></p>
<html>
<head>
<style>
table {
  border-collapse: collapse;
  width: 100%;
}

th, td {
  text-align: left;
  padding: 8px;
}

tr:nth-child(even){background-color: #f2f2f2}

th {
  background-color: #1a3152;
  color: white;
}
</style>
</head>
<body>

<h1>Professors</h1>

<table>
  <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
</table>

</body>
</html>
<table>

  <tbody>
    <% @professors.each do |professor| %>
      <tr>
        <td><%= professor.Name %></td>
        <td><%= link_to 'Show', professor %></td>
        <td><%= link_to 'Edit', edit_professor_path(professor) %></td>
        <td><%= button_to 'Destroy', professor_path(:id => professor), method: :delete%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Professor', new_professor_path %>
