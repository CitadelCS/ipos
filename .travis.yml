language: "ruby 2.6.6"
rvm:
  - 2.6.6
cache:
  - bundler
  - yarn
services:
  - postgresql
before_install:
  - nvm install --lts
before_script:
  - bundle install --jobs=3 --retry=3
  - yarn
  - rails db:create
  - rails db:schema:load
script:
  -  rails test
  -  rails test:system 
